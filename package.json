{
  "name": "plate-rank",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend",
    "mobile"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "dev:mobile": "concurrently \"npm run dev -w backend\" \"npm run start --prefix mobile\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "seed": "npm run prisma:seed -w backend",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy -w backend",
    "mobile:install": "npm install --prefix mobile",
    "mobile:start": "npm run start --prefix mobile",
    "mobile:android": "npm run android --prefix mobile",
    "mobile:ios": "npm run ios --prefix mobile",
    "mobile:typecheck": "npm run typecheck --prefix mobile",
    "mobile:build:android:apk": "npm run build:android:apk --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
